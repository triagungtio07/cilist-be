apiVersion: apps/v1
kind: Deployment
metadata:
  name: cilist-be-prod
  namespace: prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cilist-be-prod
  template:
    metadata:
      labels:
        app: cilist-be-prod
    spec:
      containers:
      - name: cilist-be-prod
        image: triagungtio/cilist-be:{{NEW_TAG}}
        ports:
        - containerPort: 5000
        envFrom:
        - configMapRef:
            name: cilist-env-prod

---
apiVersion: v1
kind: Service
metadata:
  name: cilist-be-prod
  namespace: prod
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
  selector:
    app: cilist-be-prod
